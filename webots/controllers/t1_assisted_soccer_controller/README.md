# T1 Assisted Soccer Controller

This Webots controller is used only for the assisted 2v2 physical soccer demo.
Root locomotion is supervisor-assisted. The controller provides native Webots
Motor gait animation, arm swing, push posture targets, UDP command acknowledgments,
and joint telemetry. It does not move the soccer ball.
